{
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "npm install && NODE_OPTIONS='--openssl-legacy-provider' npm run build:prod"
    },
    "deploy": {
        "startCommand": "npm run deploy"
    }
}